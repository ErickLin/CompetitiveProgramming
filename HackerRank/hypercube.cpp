#include <algorithm>
#include <cstdio>
#include <iostream>
#include <vector>

using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	unsigned int t;
	cin >> t;
	for (int i = 0; i < t; i++) {
		int n, d, k;
		cin >> n >> d >> k;
		for (int j = 0; j < k; j++) {
			
		}
	}
	return 0;
}